# Vehicle Sales Prediction — Backend dependencies
# Install with: pip install -r requirements.txt

fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0
python-multipart>=0.0.6
# .env loading is handled by backend/utils/dotenv_loader.py (zero stdlib deps — no package needed)

# ML inference (same as ml/requirements-ml.txt)
scikit-learn>=1.3.0
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Testing
pytest>=7.0.0
httpx>=0.24.0
pytest-asyncio>=0.21.0
